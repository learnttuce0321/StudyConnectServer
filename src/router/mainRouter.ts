import express, { Router } from 'express'
// import { MainIndex, PostRegisterUser } from '../controll/Cmain'

const router: Router = express.Router()

// router.get('/', MainIndex)

// router.post('/get', PostRegisterUser)

export default router